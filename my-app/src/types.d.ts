// types.d.ts makes it s declaration file
// ttherefore no need to constantly import/export new types to each file.
// ts compiler will handle this file

type NavigationLink = {
    name: string;
    path: string
};